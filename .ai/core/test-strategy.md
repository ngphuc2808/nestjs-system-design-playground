---
provenance: manual
last_verified: "2026-08-05"
archetypes: []
---

# Test Strategy

<!-- TODO: run the `reverse-search` Claude Code skill against this repo, or fill in manually.
     Each entry in `archetypes` (frontmatter above) should look like:
       - path: "apps/api/**"
         archetype: api
         test_command: "cd apps/api && npm run test"
         mcp_tools: []
       - path: "apps/web/**"
         archetype: frontend
         test_command: "cd apps/web && npm run test"
         e2e_command: "cd apps/web && npx playwright test"
         mcp_tools: [playwright]

     Archetype meaning (framework-wide, not repo-specific):
       - api:      input/output logic per feature — request/response correctness, business
                   logic across valid/invalid input cases. Typical layers: controller, service,
                   repository.
       - frontend: UI/UX behavior, conformance to core/project-rules.md conventions, and the
                   feature's actual requirement. Typical layers: component, hook, page. Uses the
                   `playwright` MCP tool for real-browser verification. -->

## Notes
[TODO — anything repo-specific about how tests are run/organized]
